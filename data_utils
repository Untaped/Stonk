import yfinance as yf

def fetch_stock_metrics(symbol):
    ticker = yf.Ticker(symbol)
    info = ticker.info
    return {
        "growth": info.get("earningsQuarterlyGrowth", 0.0),
        "dividend_yield": info.get("dividendYield", 0.0),
        "pe_ratio": info.get("trailingPE", 0.0)
    }